import { MoreHorizontal } from "lucide-react";
import { accounts, transactions } from "./data";
export function AccountList({mobile=false}:{mobile?:boolean}) { return <div className={mobile?"space-y-1.5":"space-y-2"}>{accounts.slice(0,mobile?3:2).map((a,i)=><div key={i} className={`flex items-center rounded-lg bg-white ${mobile?'min-h-[62px] px-5':'border border-line px-5 py-3'}`}><span className={`mr-5 flex h-5 w-5 items-center justify-center rounded-full text-[9px] font-bold text-white ${a.color}`}>{a.icon}</span><div className="min-w-0 flex-1"><p className="truncate text-xs font-bold">{mobile?a.short:a.number}</p><p className="mt-1 text-xs text-muted">{mobile?"Card number":"Card number"}</p></div><div className="text-right"><p className="text-xs font-bold">{a.balance}</p><p className="mt-1 text-xs text-muted">{mobile?a.valid:"Card balance"}</p></div>{!mobile&&<><div className="ml-12 hidden w-14 sm:block"><p className="text-xs font-bold">{a.valid}</p><p className="mt-1 text-xs text-muted">Valid</p></div><MoreHorizontal className="ml-8" size={16}/></>}</div>)}</div> }
export function TransactionList({short=false}:{short?:boolean}) {const data=short?transactions.slice(0,3):transactions; return <div>{data.map((t,i)=><div key={i} className="flex min-h-[55px] items-center border-b border-line last:border-0"><span className={`mr-4 flex h-7 w-7 shrink-0 items-center justify-center rounded-full text-sm font-bold text-white ${t.color}`}>{t.brand}</span><div className="min-w-0 flex-1"><p className="truncate text-xs font-bold"><span className="md:hidden">{t.mobile}</span><span className="hidden md:inline">{t.title}</span></p><p className="mt-1 text-[10px] text-muted">{t.time}</p></div><p className="ml-3 whitespace-nowrap text-xs font-bold">{t.amount}</p></div>)}</div>}
